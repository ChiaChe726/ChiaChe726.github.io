---
sidebar_position: 2
---

# Example Note

This is an example note showing what a note page looks like. Replace it with your own content.

## Code blocks

Docusaurus has built-in syntax highlighting:

```js
function greet(name) {
  return `Hello, ${name}!`;
}
```

## Admonitions

:::tip
Use admonitions like this to highlight key points.
:::

:::note
Supports note, tip, info, warning, and danger styles.
:::

## Next

Add `.md` files under `docs/` to keep expanding your notes; the sidebar updates automatically.
